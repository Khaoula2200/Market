{% set actionOptions = {'project': project.id ?? null} %}
{{ form_start(form, {
   'action': path('project_create', actionOptions),
   'attr': {'id': 'my_form'}}) }}
   {{ form_widget(form) }}
{{ form_end(form) }}